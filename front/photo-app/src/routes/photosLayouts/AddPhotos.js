import React, { useState } from 'react'

export const AddPhotos = (props) => {
  const appendNewPhotos = props.appendNewPhotos;

  const [pendingPhotos, setPendingPhotos] = useState([]);
  const uploadPhotos = () => {

  };

  return (
    <div></div>
  )
}
